# Expo HAS CHANGED

This folder runs **Expo SDK 56**. Read the exact versioned docs at
https://docs.expo.dev/versions/v56.0.0/ before writing any code.
Do NOT downgrade Expo / React Native (`expo ^56.0.15`, `react-native 0.85.3`).
