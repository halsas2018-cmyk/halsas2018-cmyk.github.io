// Shared data + helpers for the laws-of-reflection practical.
export const ANGLE_CHOICES = [20, 30, 40, 50, 60];

// By the law of reflection the reflected angle equals the incident angle.
export function reflectedAngle(thetaI) {
  return thetaI;
}
