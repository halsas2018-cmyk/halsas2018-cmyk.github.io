import sizeHeightWeight from "./sizeHeightWeight.js";
import fingerprints from "./fingerprints.js";

// Keys MUST match the subtopic strings in constants.js exactly.
export default {
  "Size/height/weight": sizeHeightWeight,
  "Fingerprints": fingerprints,
};
