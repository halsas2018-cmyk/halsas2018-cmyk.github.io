import tolerance from "./Tolerance.js";
import minimumMaximum from "./MinimumMaximum.js";

// Keys MUST match the subtopic strings in constants.js exactly.
export default {
  Tolerance: tolerance,
  "Minimum/maximum": minimumMaximum,
};
